% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_inputs_discrete.R
\name{prepare_inputs_discrete}
\alias{prepare_inputs_discrete}
\alias{prepare_invasive_mechanical_ventilation_indicator}
\alias{prepare_o2support}
\alias{prepare_dobutamine}
\alias{prepare_dopamine}
\alias{prepare_epinephrine}
\alias{prepare_milrinone}
\alias{prepare_norepinephrine}
\alias{prepare_vasopressin}
\alias{prepare_gcseye}
\alias{prepare_gcsmotor}
\alias{prepare_gcsverbal}
\alias{prepare_gcstotal}
\alias{prepare_pupilleft}
\alias{prepare_pupilright}
\alias{prepare_pupils}
\alias{prepare_antimicrobials}
\alias{prepare_antiinfectioustests}
\title{Prepare Discrete Observation, Intervention, Event, and Test Inputs for
Phoenix Scoring}
\usage{
prepare_invasive_mechanical_ventilation_indicator(
  x,
  id.vars,
  eclock,
  value.var,
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_o2support(
  x,
  id.vars,
  eclock,
  value.var,
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_dobutamine(
  x,
  id.vars,
  eclock,
  value.var,
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_dopamine(
  x,
  id.vars,
  eclock,
  value.var,
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_epinephrine(
  x,
  id.vars,
  eclock,
  value.var,
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_milrinone(
  x,
  id.vars,
  eclock,
  value.var,
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_norepinephrine(
  x,
  id.vars,
  eclock,
  value.var,
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_vasopressin(
  x,
  id.vars,
  eclock,
  value.var,
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_gcseye(
  x,
  id.vars,
  eclock,
  value.var,
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_gcsmotor(
  x,
  id.vars,
  eclock,
  value.var,
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_gcsverbal(
  x,
  id.vars,
  eclock,
  value.var,
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_gcstotal(
  x,
  id.vars,
  eclock,
  value.var,
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_pupilleft(
  x,
  id.vars,
  eclock,
  value.var,
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_pupilright(
  x,
  id.vars,
  eclock,
  value.var,
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_pupils(
  x,
  id.vars,
  eclock,
  value.var,
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_antimicrobials(
  x,
  id.vars,
  eclock,
  value.var,
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_antiinfectioustests(
  x,
  id.vars,
  eclock,
  value.var,
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)
}
\arguments{
\item{x}{a data.frame, or an object that inherits from a data.frame such as a
data.table or tibble, containing one Phoenix input in long format.}

\item{id.vars}{a character vector of one or more column names in \code{x}
that identify the encounter. Common examples are site, patient, and
encounter identifiers. The same \code{id.vars}, in the same order, must be
used for all inputs that will be combined with
\code{\link{prepare_phoenix_data}}.}

\item{eclock}{a character vector of length one naming the encounter-clock
column in \code{x}. The encounter clock is expected to be numeric, usually
minutes from encounter start, with 0 denoting the start of the encounter.
Age is static for the encounter and ignores \code{eclock}.}

\item{value.var}{a character vector of length one naming the column in
\code{x} that contains the observed value for the Phoenix input being
prepared.}

\item{tie.breaker}{a function used when more than one row has the same
encounter identifier and encounter-clock value. The default is chosen
separately for each Phoenix input so duplicate observations are resolved in
the clinically conservative direction.}

\item{verbose}{when \code{TRUE}, print messages describing validation,
duplicate handling, and preparation progress.}
}
\description{
Functions for checking the basic structure and values for Phoenix inputs with
fixed allowable values.
}
\details{
The input data is expected to be in a "long" format with \code{id.vars}
(examples: hospital id, patient id, encounter id). A column for reporting the
amount of time from admission, \code{eclock}
('encounter clock'; generally expected to be in minutes with 0 being the encounter start).
\code{value.var} denotes the reported values for the input of interest.

These functions validate against input-specific fixed allowable values such
as \code{c(0, 1)} for indicators or the known score sets for GCS
components/subscores. End users are not expected to change those allowable
values.

There is an expectation when going to Phoenix scoring that the
\code{x[[c(id.vars, eclock)]]} are unique for each input.  These functions
check this assumption and will aggregate, if needed, using the
\code{tie.breaker} method.

Default values are based on the values used when building the Phoenix
criteria, see Sanchez-Pinto, Bennett, DeWitt, Russell, et al. (2024).
}
\references{
See reference details in \code{\link{phoenix-package}} or by calling
\code{citation('phoenix')}.
}
