% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_inputs_range.R
\name{prepare_inputs_range}
\alias{prepare_inputs_range}
\alias{prepare_fio2}
\alias{prepare_spo2}
\alias{prepare_pao2}
\alias{prepare_mean_airway_pressure_ventilator}
\alias{prepare_mean_airway_pressure_hfov}
\alias{prepare_positive_end_expiratory_pressure}
\alias{prepare_lactate}
\alias{prepare_sbp_cuff}
\alias{prepare_sbp_arterial}
\alias{prepare_dbp_cuff}
\alias{prepare_dbp_arterial}
\alias{prepare_mean_arterial_pressure_cuff}
\alias{prepare_mean_arterial_pressure_arterial}
\alias{prepare_platelets}
\alias{prepare_inr}
\alias{prepare_ddimer}
\alias{prepare_fibrinogen}
\alias{prepare_glucose}
\alias{prepare_anc}
\alias{prepare_alc}
\alias{prepare_creatinine}
\alias{prepare_bilirubin}
\alias{prepare_alt}
\alias{prepare_age}
\title{Prepare Continuous Observation, Intervention, Medication, and Test Inputs for
Phoenix Scoring}
\usage{
prepare_fio2(
  x,
  id.vars,
  eclock,
  value.var,
  valid.range = c(0.21, 1),
  valid.range.closed = c(TRUE, TRUE),
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_spo2(
  x,
  id.vars,
  eclock,
  value.var,
  valid.range = c(0, 100),
  valid.range.closed = c(TRUE, TRUE),
  tie.breaker = min,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_pao2(
  x,
  id.vars,
  eclock,
  value.var,
  valid.range = c(0, Inf),
  valid.range.closed = c(TRUE, TRUE),
  tie.breaker = min,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_mean_airway_pressure_ventilator(
  x,
  id.vars,
  eclock,
  value.var,
  valid.range = c(0, Inf),
  valid.range.closed = c(TRUE, TRUE),
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_mean_airway_pressure_hfov(
  x,
  id.vars,
  eclock,
  value.var,
  valid.range = c(0, Inf),
  valid.range.closed = c(TRUE, TRUE),
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_positive_end_expiratory_pressure(
  x,
  id.vars,
  eclock,
  value.var,
  valid.range = c(0, 100),
  valid.range.closed = c(TRUE, TRUE),
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_lactate(
  x,
  id.vars,
  eclock,
  value.var,
  valid.range = c(0, 50),
  valid.range.closed = c(TRUE, TRUE),
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_sbp_cuff(
  x,
  id.vars,
  eclock,
  value.var,
  valid.range = c(1, 300),
  valid.range.closed = c(TRUE, TRUE),
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_sbp_arterial(
  x,
  id.vars,
  eclock,
  value.var,
  valid.range = c(1, 300),
  valid.range.closed = c(TRUE, TRUE),
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_dbp_cuff(
  x,
  id.vars,
  eclock,
  value.var,
  valid.range = c(1, 200),
  valid.range.closed = c(TRUE, TRUE),
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_dbp_arterial(
  x,
  id.vars,
  eclock,
  value.var,
  valid.range = c(1, 200),
  valid.range.closed = c(TRUE, TRUE),
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_mean_arterial_pressure_cuff(
  x,
  id.vars,
  eclock,
  value.var,
  valid.range = c(1, 300),
  valid.range.closed = c(TRUE, TRUE),
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_mean_arterial_pressure_arterial(
  x,
  id.vars,
  eclock,
  value.var,
  valid.range = c(1, 300),
  valid.range.closed = c(TRUE, TRUE),
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_platelets(
  x,
  id.vars,
  eclock,
  value.var,
  valid.range = c(0, Inf),
  valid.range.closed = c(TRUE, TRUE),
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_inr(
  x,
  id.vars,
  eclock,
  value.var,
  valid.range = c(0, Inf),
  valid.range.closed = c(TRUE, TRUE),
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_ddimer(
  x,
  id.vars,
  eclock,
  value.var,
  valid.range = c(0, 500),
  valid.range.closed = c(TRUE, TRUE),
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_fibrinogen(
  x,
  id.vars,
  eclock,
  value.var,
  valid.range = c(0, Inf),
  valid.range.closed = c(TRUE, TRUE),
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_glucose(
  x,
  id.vars,
  eclock,
  value.var,
  valid.range = c(5, 2000),
  valid.range.closed = c(TRUE, TRUE),
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_anc(
  x,
  id.vars,
  eclock,
  value.var,
  valid.range = c(0, Inf),
  valid.range.closed = c(TRUE, TRUE),
  tie.breaker = min,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_alc(
  x,
  id.vars,
  eclock,
  value.var,
  valid.range = c(0, Inf),
  valid.range.closed = c(TRUE, TRUE),
  tie.breaker = min,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_creatinine(
  x,
  id.vars,
  eclock,
  value.var,
  valid.range = c(0, 50),
  valid.range.closed = c(TRUE, TRUE),
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_bilirubin(
  x,
  id.vars,
  eclock,
  value.var,
  valid.range = c(0, 100),
  valid.range.closed = c(TRUE, TRUE),
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_alt(
  x,
  id.vars,
  eclock,
  value.var,
  valid.range = c(0, Inf),
  valid.range.closed = c(TRUE, TRUE),
  tie.breaker = max,
  verbose = getOption("phoenix_verbose", interactive())
)

prepare_age(
  x,
  id.vars,
  value.var,
  valid.range = c(0, 216),
  valid.range.closed = c(TRUE, FALSE),
  tie.breaker = min,
  verbose = getOption("phoenix_verbose", interactive())
)
}
\arguments{
\item{x}{a data.frame, or an object that inherits from a data.frame such as a
data.table or tibble, containing one Phoenix input in long format.}

\item{id.vars}{a character vector of one or more column names in \code{x}
that identify the encounter. Common examples are site, patient, and
encounter identifiers. The same \code{id.vars}, in the same order, must be
used for all inputs that will be combined with
\code{\link{prepare_phoenix_data}}.}

\item{eclock}{a character vector of length one naming the encounter-clock
column in \code{x}. The encounter clock is expected to be numeric, usually
minutes from encounter start, with 0 denoting the start of the encounter.
Age is static for the encounter and ignores \code{eclock}.}

\item{value.var}{a character vector of length one naming the column in
\code{x} that contains the observed value for the Phoenix input being
prepared.}

\item{valid.range}{a numeric vector of length two defining the allowable
interval for \code{x[[value.var]]}. Defaults are input-specific and match
the operational Phoenix definition. Users may pass a different interval when
their source data require a different validation rule.}

\item{valid.range.closed}{A logical vector of length one or two denoting
whether the lower and upper bounds of \code{valid.range} are closed
(inclusive) or open (exclusive). A length-one value is recycled for both
bounds. Most inputs default to \code{c(TRUE, TRUE)}. \code{prepare_age()}
defaults to \code{c(TRUE, FALSE)} to match the expected age interval of
[0, 216) months.}

\item{tie.breaker}{a function used when more than one row has the same
encounter identifier and encounter-clock value. The default is chosen
separately for each Phoenix input so duplicate observations are resolved in
the clinically conservative direction.}

\item{verbose}{when \code{TRUE}, print messages describing validation,
duplicate handling, and preparation progress.}
}
\description{
Functions for checking the basic structure and values for the input data for
Assessing Phoenix.
}
\details{
The input data is expected to be in a "long" format with \code{id.vars}
(examples: hospital id, patient id, encounter id). A column for reporting the
amount of time from admission, \code{eclock}
('encounter clock'; generally expected to be in minutes with 0 being the encounter start).
\code{value.var} denotes the reported values for the input of interest.
\code{valid.range} is used for simple inclusive checks for valid values via
\code{x[[value.var]] >= min(valid.range) & x[[value.var]] <= max(valid.range)}.
\code{prepare_age()} is the exception; its default upper limit is exclusive
to match the expected age interval of [0, 216) months.

There is an expectation when going to Phoenix scoring that the
\code{x[[c(id.vars, eclock)]]} are unique for each input.  These functions
check this assumption and will aggregate, if needed, using the
\code{tie.breaker} method.

Every function below is a thin wrapper around `prepare_variable()`.  The
wrapper's main job is to provide the Phoenix-internal variable name, the
clinically expected range, and a duplicate-row tie breaker.  For example,
`prepare_spo2()` maps a user-supplied value column to the internal column
`SPO2`, checks that values are between 0 and 100, and uses the lower value
when duplicate SpO2 values are reported for the same encounter/time.

These wrappers are intentionally repetitive.  Keeping each input explicit
makes it easier for analysts to confirm which raw EHR field maps to which
Phoenix definition and TeX equation.

Default values match the values used when developing the Phoenix Sepsis
Criteria, see Sanchez-Pinto, Bennett, DeWitt, Russell, et al. (2024). End
users can modify these checks by passing a different \code{valid.range}.
}
\references{
See reference details in \code{\link{phoenix-package}} or by calling
\code{citation('phoenix')}.
}
